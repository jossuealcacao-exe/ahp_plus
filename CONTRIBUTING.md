# Contributing

This prerelease is not open for external redistribution or contribution until
the owner selects a public license. Local development must preserve protocol
semantics, add tests for behavior changes, and pass:

```bash
npm test
npm run conformance
npm run validate
npm run pack:dry-run
```

Commits, pushes, tags, releases, and package publication require explicit owner
approval.
